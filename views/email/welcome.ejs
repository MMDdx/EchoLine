<!doctype html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width"/>
    <title><%= appName %> — Confirm your email</title>
    <style>
        /* Simple inline-friendly email styles */
        body { background:#f6f8fa; font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial; margin:0; padding:20px; color:#333; }
        .email-wrap{ max-width:680px; margin:0 auto; background:#ffffff; border-radius:8px; overflow:hidden; box-shadow:0 8px 30px rgba(0,0,0,0.06);}
        .header{ padding:24px; text-align:left; background:linear-gradient(90deg,#071019,#0f2430); color:#fff; }
        .logo { font-weight:700; font-size:18px; letter-spacing:0.3px; }
        .container{ padding:28px; }
        h1 { margin:0 0 12px 0; font-size:20px; color:#0b2a2f; }
        p { margin:0 0 14px 0; line-height:1.45; color:#444; }
        .btn-wrap { margin:18px 0; text-align:center; }
        .btn {
            display:inline-block;
            background:#00b37e;
            color:#fff;
            text-decoration:none;
            padding:12px 22px;
            border-radius:8px;
            font-weight:600;
            box-shadow: 0 6px 18px rgba(0,179,126,0.18);
        }
        .muted { color:#888; font-size:13px; margin-top:18px; }
        .footer { padding:18px 24px; font-size:12px; color:#7a7a7a; background:#fbfbfb; text-align:center; }
        .fallback { word-break:break-word; color:#006064; }
        @media (max-width:480px){ .container{ padding:18px } .btn{ width:100%; box-sizing:border-box } }
    </style>
</head>
<body>
<div class="email-wrap">
    <div class="header">
        <div class="logo"><%= appName %></div>
    </div>

    <div class="container">
        <h1><%= subject %></h1>

        <p>Hi <strong><%= name || 'there' %></strong>,</p>

        <p>Thanks for signing up for <strong><%= appName %></strong>. Please confirm your email address to activate your account.</p>

        <div class="btn-wrap">
            <a class="btn" href="<%= url %>">Confirm my email</a>
        </div>

        <p class="muted">
            The link above will expire in 30 Minutes. If the button doesn't work, paste the following link into your browser:
        </p>

        <p class="fallback"><a href="<%= url %>"><%= url %></a></p>

        <p style="margin-top:18px">If you didn't create an account, you can safely ignore this message.</p>
    </div>

    <div class="footer">
        <div><%= appName %>© • created by Mohammad Kabiri</div>
    </div>
</div>
</body>
</html>
